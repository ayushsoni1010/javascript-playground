// 50. Pow(x, n)

// Q. Implement pow(x, n), which calculates x raised to the power n (i.e., xn).

/**
 * @param {number} x
 * @param {number} n
 * @return {number}
 */
function myPowTS(x: number, n: number): number {
  return Math.pow(x, n);
}
